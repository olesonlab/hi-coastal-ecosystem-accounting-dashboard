# 

# Purpose

# Rules

# Current Status

# Imports
box::use(
  shiny[moduleServer, NS],
)
# Modules
box::use(
  
)

#' @export
ui <- function(id) {
  ns <- NS(id)
}

#' @export
server <- function(id) {
  moduleServer(id, function(input, output, session) {
  })
}